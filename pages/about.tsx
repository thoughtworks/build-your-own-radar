import { NextPage } from 'next';

import { Layout } from '../components';

const About: NextPage = () => {
    return (
        <Layout>
            <h1>About page</h1>
        </Layout>
    );
};

export default About;
