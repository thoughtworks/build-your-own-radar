import { NextPage } from 'next';

import { Layout } from '../components';

const Stacks: NextPage = () => {
    return (
        <Layout>
            <h1>Stacks page</h1>
        </Layout>
    );
};

export default Stacks;
